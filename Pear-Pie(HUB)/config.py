# =============================================================================
# config.py  (HUB)
# =============================================================================
# Hub-wide settings.
# =============================================================================

LOG_FILE   = "pod_log.csv"     # where pod data is logged
MODEL_FILE = "model.pkl"       # where a trained model gets saved (later)