# compute parameter updates (alpha, threshold, r) and broadcast them back down down to pods.
# Computes small updates (alpha, threshold) from what the hub learned,
# to broadcast back down to pods. The most advanced piece - build last.
def compute_updates(model):
    # TODO
    pass